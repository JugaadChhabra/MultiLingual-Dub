[pytest]
# The repo has no installable package — api/, services/ and batch/ are imported
# as top-level modules from the working tree. `python -m pytest` happens to put
# the root on sys.path, but a bare `pytest` does not, so without this the whole
# suite fails to import depending on how it was invoked.
pythonpath = .
testpaths = tests
